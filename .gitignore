# ─────────────────────────────────────────────────────────────────────────────
# Node / React
# ─────────────────────────────────────────────────────────────────────────────
node_modules/              # packages
.yarn/                     # Yarn 2+
/dist/                     # TS build outputs
/build/                    # CRA build
/.next/                    # Next.js
/out/                      # other outputs

# lockfiles
package-lock.json
# yarn.lock
pnpm-lock.yaml

# env files
.env
.env.*.local
!.env.example             # keep an example

# logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
*.log

# coverage
coverage/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.suo
*.njsproj
*.ntvs*
*.sln

# TypeScript build cache
*.tsbuildinfo

# ─────────────────────────────────────────────────────────────────────────────
# Anchor / Solana / Rust
# ─────────────────────────────────────────────────────────────────────────────
.solana/                  # CLI artifacts (optional)
test-ledger/              # local validator ledger
anchor.lock               # Anchor’s lockfile
.anchor/                  # if you see it in your workspace

# Rust/Cargo build artifacts
/target/
**/target/
programs/**/target/
**/*.so

# Anchor BPF & IDL
programs/**/target/       # program build artifacts
**/*.so                   # BPF binaries
target/deploy/*.so
target/idl/               # generated IDLs
idl/*.json

# Solana keypairs
*.keypair
*.json                    # if you store keypairs as JSON

# Anchor debug log
anchor-debug.log

